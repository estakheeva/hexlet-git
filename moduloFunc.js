const ev = (a, n) => {
  console.log(a % n);
};
ev(17, 3); // 2
// определить чётность (x % 2 === 0)
//выполнять действия “каждую n-ю итерацию”
//\для циклических структур (например, индексы в массивах).
